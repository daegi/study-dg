package com.test0422;

/*
public class Exam {
	- 키보드로 한문자 입력 받기(ASCII 코드)
	int data = System.in.read();
	
	- 파일에서 내용 읽기
	byte[] b = new Byte[1024];
	int len;
	
	FileInputStream fis= new FileInputStream("파일명");
	또는
	File f = new File(FileInputStream fis= new FileInputStream(f);
	
	whille((len = fis.read(b,0,1024))!= -1){
		//처리
	}
	fls.close();
	
	-파일에 저장하기
	FileOutputStream fos = new FileOutputStream("파일명");
	또는
	File f= new File(FileOutputStream fos= new FileOutputStream(f);
	
	while((len = fous.read(b,0,1024))!= -1){
		fos.write(b,0,len);
	}
	fos.close();
	fis.close();
-----------------------------------------------------------

while((data=System.in.read()) != -1){
	fos.write(data);
}

------------------------------------------------------------

ArrayList, HashMap, Iterator <진짜 중요>
try~ catch, 제어문, String StringBuffer

상속, 인터페이스



	
	
*/	
